const nome = "Rafael"; // String
const numero = 10; // number
const numero2 = 10.1; // number
let indefinido; // undefined = Não aponta para nenhum local da memória
let nulo = null; // Nulo

// Booleanos
const verdadeiro = true;
const falso = false;
