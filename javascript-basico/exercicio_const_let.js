const nome = "Rafael";
const idade = 30;
const peso = 80;
const altura = 1.64;
const imc = peso / (altura * altura);

console.log(`
  ${nome} tem ${idade} anos, pesa ${peso}Kg tem ${altura} de altura.
  Seu IMC é: ${imc}
`);
